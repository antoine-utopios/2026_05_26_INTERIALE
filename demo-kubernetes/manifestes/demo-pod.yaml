apiVersion: v1
kind: Pod
metadata:
  name: demo-pod
  labels:
    tier: frontend
    app: nginx
spec:
  containers:
  - name: demo-container
    image: nginx:alpine
    ports:
      - containerPort: 80
