apiVersion: v1
kind: Service
metadata:
  name: demo-service
spec:
  type: NodePort
  selector:
    tier: frontend
    app: nginx
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30001 # 30000-32767
